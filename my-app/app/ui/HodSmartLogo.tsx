import { lusitana } from "./fonts"
export default function HodSmartLogo(){
    return(
        <h1 className={`${lusitana.className} font-bold text-white text-3xl`}>
            HOD Smart
        </h1>
    )
}